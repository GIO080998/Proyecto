<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="MDGF.edu.tesoem.itics.proyectomdgf">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".impresoraActivity"></activity>
        <activity android:name=".sillaActivity" />
        <activity android:name=".tecladoActivity" />
        <activity android:name=".mouseActivity" />
        <activity android:name=".lenovoActivity" />
        <activity android:name=".dellActivity" />
        <activity android:name=".huaweiActivity" />
        <activity android:name=".acerActivity" />
        <activity android:name=".macActivity" />
        <activity android:name=".hpActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>